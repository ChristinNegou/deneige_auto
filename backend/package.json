{
  "name": "deneige-auto-backend",
  "version": "1.0.0",
  "description": "Backend API pour l'application Déneige Auto",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js --watch . --ext js,json",
    "dev:verbose": "nodemon server.js --watch . --ext js,json --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["api", "deneigement", "backend"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "mongoose": "^8.9.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "dotenv": "^16.4.7",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}